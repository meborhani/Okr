import type { Objective } from '@/types';
interface ObjectiveCardProps {
    objective: Objective;
}
export declare function ObjectiveCard({ objective }: ObjectiveCardProps): import("react").JSX.Element;
export {};
